export enum ImageName {
  MECA_EXECUTOR = 'ghcr.io/sbip-sg/meca-executor:main',
  PYMECA_SERVER = 'mock_actor_intern:latest',
}

export enum ContainerName {
  MECA_EXECUTOR_1 = 'meca-executor-1',
  PYMECA_SERVER_1 = 'pymeca-server-1',
}

export enum ContainerPort {
  MECA_EXECUTOR_1_PORT = '2591',
  PYMECA_SERVER_1_PORT = '9999',
}
